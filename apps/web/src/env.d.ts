/// <reference types="vite/client" />

// Injected by vite.config.ts (define) from desktop/app/tauri.conf.json so the
// header badge always shows the shipped app version.
declare const __APP_VERSION__: string;
